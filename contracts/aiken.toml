# dependencies removed to allow offline/local-only build
# { name = "aiken/collection", version = ">=1.0.0", source = "github" },
# { name = "aiken/cardano", version = ">=1.0.0", source = "github" },
# { name = "sian_lab/vodka", version = ">=1.0.0", source = "github" },
name = "messi/cardano-escrow"
version = "0.1.0"
description = "Escrow validator for Cardano freelance payments"

